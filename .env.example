# Copy to .env — secrets only. Everything else belongs in config.toml.

# Discord bot token (phase 5; leave blank until then — the CLI works without it)
DISCORD_TOKEN=

# Optional: override where state is written. Defaults to ./data
# OWWATCH_HOME=C:\Users\admin\Desktop\ow-watch-bot
